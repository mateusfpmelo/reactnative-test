import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        padding: 32,
        gap:8,
    },
    title: {
        color: "#251893",
        fontSize: 24,
        fontWeight: "bold",
    },
    subtitle: {
        color: "#7F58FF",
        fontSize: 18,
        fontWeight: "bold",
    },
})