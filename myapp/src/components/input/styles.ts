import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        height: 52,
        width: '100%',
        padding: 16,
        borderRadius: 8,
        fontSize: 16,
        borderWidth: 1,
        borderColor: "#7F58FF",
    }
})